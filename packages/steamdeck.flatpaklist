# Flatpak apps installed on steamdeck hosts.
# Used because some apps (wezterm) ship as macOS-only Casks on Homebrew and
# need a Linux install path. Installed user-scope from flathub.
# One app ID per line. Blank lines and lines starting with `#` are ignored.

org.wezfurlong.wezterm
