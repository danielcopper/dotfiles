[http]
	lowSpeedLimit = 1000
	lowSpeedTime = 60
	postBuffer = 524288000

# [user] is host-specific (work-default on wsl, personal on arch/steamdeck).
# Set in host-<class>/.gitconfig.local via [include] below.

[include]
	path = ~/.gitconfig.local
