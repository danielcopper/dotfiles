# mise shims on PATH for the whole user session, not just interactive bash
# (where `mise activate` runs). Non-interactive shells and hook commands —
# e.g. Claude Code hooks calling tools installed through mise — need it.
PATH=${HOME}/.local/share/mise/shims:${PATH}
